import { LeftDragDirective } from './left-drag.directive';

describe('LeftDragDirective', () => {
  it('should create an instance', () => {
    const directive = new LeftDragDirective();
    expect(directive).toBeTruthy();
  });
});
