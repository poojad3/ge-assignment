import { RightDropDirective } from './right-drop.directive';

describe('RightDropDirective', () => {
  it('should create an instance', () => {
    const directive = new RightDropDirective();
    expect(directive).toBeTruthy();
  });
});
